<%= render 'layout/search_ribbon', locals: locals %>

<div class="container flex-container">


  <main class="goal-result-container">
    <ul class="goal-result-list">

      <li class="goal-result">
        <h2 class="goal-title">Goal: <%= goal.title %></h2>
        <dl class="goal-metadata-list">
          <dt>Scope</dt>
          <dd>
            <% if goal.scope_links.empty? %>
              <%= goal.creator.name %>
            <% else %>
              <% goal.scope_links.each do |scope| %>
                <a href="<%= scope.url %>"><%= scope.name %></a><br>
              <% end %>
            <% end %>
          </dd>


          <dt>

          </dt>
          <dd>
            <%= goal.description %>
          </dd>

          <% if !goal.stewards_list.empty? %>
            <dt>
              Stewards
            <dt>
            <dd>
              <%= goal.stewards_list %>
            </dd>
          <% end %>

          <dt>
            Status
          <dt>
          <dd>
            <%= goal.status %>
          </dd>

          <dt>
            Target Date
          <dt>
          <dd>
            <%= goal.target_date %>
          </dd>

          <% if !goal.associated_goals.empty? %>
            <dt>
              Associated Goals
            <dt>
            <dd>
              <ul class="associated-goals-list">
                <% goal.associated_goals.each do |ag| %>
                  <li>
                    <a href="<%= ag.url %>"><%= ag.title %></a>
                  </li>
                <% end %>
              </ul>
            </dd>
          <% end %>
        </dl>

      </li>

    </ul>
  </main>
</div>




